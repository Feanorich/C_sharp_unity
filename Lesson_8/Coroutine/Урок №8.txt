Реализовал паузу/продолжение и финиш для корутины.

Для отладки немного модифицировал запускаемый в корутине IEnumerator Timer
И создал ExerciseCoroutine : MonoBehaviour, в качестве точки входа.

Так как к основному проекту ничего не добавил, то использовал тут кортеж
и разделение ответственностей, чтобы показать, насколько понял тему.
